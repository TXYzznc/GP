# Unity打包后问题排查指南

## 问题症状
1. 渲染效果与编辑器不一致
2. 部分图片资源未加载
3. 场景对象（敌人、宝箱）未生成

---

## 快速解决方案

### 步骤1：开启Development Build（开发者构建）

1. **打开App Builder窗口**
2. **切换到"Debug Mode"**（在窗口底部）
3. **点击Player Settings**，勾选以下选项：
   - ☑ Development Build
   - ☑ Script Debugging
   - ☑ Wait For Managed Debugger（如果需要调试代码）

4. **重新Build App**

**效果**：
- 可以查看详细的错误日志
- 可以按 F1 查看运行时日志
- 可以使用 `Debug.Log` 输出信息

---

### 步骤2：检查PlayerLog日志

打包后的应用日志位置：
```
Windows: C:\Users\<用户名>\AppData\LocalLow\<公司名>\<产品名>\Player.log
Mac: ~/Library/Logs/<公司名>/<产品名>/Player.log
```

你的项目日志路径：
```
C:\Users\WIN10\AppData\LocalLow\珍珠奶茶\Clash_Of_Gods\Player.log
```

**重点查找**：
- `FileNotFoundException` - 文件未找到
- `NullReferenceException` - 空引用（资源加载失败）
- `AssetBundle` 相关错误
- 配置表加载失败的提示

---

## 常见问题及解决方案

### 问题1：资源未加载（图片、Prefab等）

#### 原因：
GameFramework的资源模式问题。编辑器模式下直接从Assets加载，打包后必须从AssetBundle加载。

#### 检查点：
```csharp
// 检查当前资源模式
GameEntry.Resource.ResourceMode
// 应该是 ResourceMode.Package（打包模式）
```

#### 解决方案A：确保资源已打包
1. 打开 **Resource Rule Editor**
2. 点击 **Refresh ResourceCollection.xml**
3. 点击 **Build Resources**
4. 确认 `AB/Package/1_0_4_0/` 目录下有对应的 `.dat` 文件

#### 解决方案B：检查资源路径
确保代码中使用的资源路径与ResourceCollection.xml中一致：
```csharp
// 正确的路径格式（不带扩展名）
GameEntry.Resource.LoadAsset("AAAGame/Sprites/Card/策略卡/不屈意志", ...);

// 错误示例（不要加.png）
GameEntry.Resource.LoadAsset("AAAGame/Sprites/Card/策略卡/不屈意志.png", ...); // ❌
```

---

### 问题2：场景对象未生成

#### 原因：
配置表或Prefab资源加载失败，导致后续生成逻辑中断。

#### 排查步骤：
1. **检查配置表是否加载成功**
   ```csharp
   // 在生成逻辑前添加日志
   DebugEx.Log("MapSpawn", $"配置表行数: {GameEntry.DataTable.GetDataTable<DRMapSpawnTable>()?.Count ?? 0}");
   ```

2. **检查Prefab路径**
   ```csharp
   // 确认Prefab路径正确
   string prefabPath = "AAAGame/Prefabs/Enemy/邪灵";
   DebugEx.Log("Spawn", $"准备加载Prefab: {prefabPath}");
   ```

3. **异步加载完成后再使用**
   ```csharp
   // 确保await完成
   await GameEntry.Resource.LoadAssetAsync<GameObject>(prefabPath);
   ```

---

### 问题3：渲染效果差异

#### 原因：
Quality Settings在编辑器和打包后使用的Level不同。

#### 解决方案：
1. **打开** `Edit > Project Settings > Quality`
2. **检查默认Quality Level**：
   - 编辑器默认：通常是 "Ultra" 或 "Very High"
   - Standalone默认：可能被设置为 "Medium" 或 "High"

3. **统一Quality Level**：
   - 在Standalone列，选择与编辑器相同的Quality Level
   - 或者在代码中强制设置：
     ```csharp
     void Start()
     {
         QualitySettings.SetQualityLevel(5); // 5 = Ultra
     }
     ```

4. **检查Graphics Settings**：
   - `Edit > Project Settings > Graphics`
   - 确认 URP Asset 配置正确
   - 检查 Scriptable Render Pipeline Settings

---

## 高级调试技巧

### 技巧1：运行时资源检查器

在游戏启动时添加资源加载检查：

```csharp
public class ResourceChecker : MonoBehaviour
{
    void Start()
    {
        StartCoroutine(CheckResources());
    }

    IEnumerator CheckResources()
    {
        yield return new WaitForSeconds(2f);
        
        Debug.Log($"=== 资源检查 ===");
        Debug.Log($"资源模式: {GameEntry.Resource.ResourceMode}");
        Debug.Log($"适用版本: {GameEntry.Resource.ApplicableGameVersion}");
        Debug.Log($"内部版本: {GameEntry.Resource.InternalResourceVersion}");
        
        // 检查关键资源
        CheckAsset("AAAGame/Sprites/Card/策略卡/不屈意志");
        CheckAsset("AAAGame/Prefabs/Enemy/邪灵");
    }

    async void CheckAsset(string assetPath)
    {
        try
        {
            var asset = await GameEntry.Resource.LoadAssetAsync<UnityEngine.Object>(assetPath);
            Debug.Log($"✅ 资源存在: {assetPath}");
        }
        catch (Exception ex)
        {
            Debug.LogError($"❌ 资源缺失: {assetPath} | 错误: {ex.Message}");
        }
    }
}
```

### 技巧2：对比编辑器和打包模式

在 `GameEntry` 或启动脚本中添加：

```csharp
void Awake()
{
    #if UNITY_EDITOR
        Debug.Log("运行模式: 编辑器");
    #else
        Debug.Log("运行模式: 打包后应用");
    #endif
    
    Debug.Log($"当前场景: {UnityEngine.SceneManagement.SceneManager.GetActiveScene().name}");
    Debug.Log($"资源模式: {GameEntry.Resource.ResourceMode}");
}
```

---

## 完整排查清单

打包前检查：
- [ ] Refresh ResourceCollection.xml
- [ ] Build Resources成功
- [ ] 确认 AB/Package/1_0_4_0/ 目录有.dat文件
- [ ] 资源路径不包含文件扩展名
- [ ] Quality Settings统一

打包后检查：
- [ ] 使用Development Build
- [ ] 查看Player.log日志
- [ ] 确认ResourceMode为Package
- [ ] 运行资源检查器
- [ ] 对比编辑器和打包日志

---

## 终极方案：Editor模拟打包模式

如果想在编辑器中测试打包效果，可以强制使用Package模式：

1. **找到资源模式配置**
   - 通常在 `GameEntry` 或 `LaunchProcedure` 中

2. **临时修改为Package模式**
   ```csharp
   // 在编辑器中也使用打包模式
   #if UNITY_EDITOR
       GameEntry.Resource.SetResourceMode(ResourceMode.Package);
   #endif
   ```

3. **测试完成后记得改回来**

---

## 联系支持

如果以上方案都无法解决，请提供：
1. Player.log 日志文件
2. 具体是哪些资源未加载（截图）
3. ResourceCollection.xml 文件
4. 场景生成相关代码

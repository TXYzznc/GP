# 新卡牌效果实现 - 完成总结

> **时间**: 2026-04-17  
> **阶段**: Phase 1 & 2 完成，等待 Phase 3（配置确认）  
> **状态**: 代码实现完毕

---

## 已完成工作

### Phase 1: 新增选择器（3个） ✅

在 `TargetSelectors.cs` 中添加了 3 个缺失的选择器：

#### 1. SelfSelector - 自身选择器
- 获取施法者（召唤师）自身
- 用于卡牌 1013（反伤之盾）、1008（狂暴）、1026（伤害转移）

#### 2. AllAllyExcludeSummonerSelector - 全友（不含召唤师）
- 选择全体友方单位（排除召唤师）
- 用于需要保护友军但不影响召唤师的卡牌

#### 3. AlliesInRadiusSelector - 范围内友方选择器
- 在指定范围内选择所有友方单位
- 用于卡牌 1014（圣域防线）

**覆盖情况：** 现已覆盖全部 8 种目标类型

| 目标类型 | 枚举值 | 选择器 | ✅ |
|---------|--------|--------|---|
| 1. Self | 1 | SelfSelector | ✅ |
| 2. AllAllyExcludeSummoner | 2 | AllAllyExcludeSummonerSelector | ✅ |
| 3. AllAlly | 3 | AllAlliesSelector | ✅ |
| 4. AllEnemy | 4 | AllEnemiesSelector | ✅ |
| 5. SingleAlly | 5 | ClosestAllySelector | ✅ |
| 6. AreaAlly | 6 | AlliesInRadiusSelector | ✅ |
| 7. AreaEnemy | 7 | EnemiesInRadiusSelector | ✅ |
| 8. SingleEnemy | 8 | ClosestEnemySelector | ✅ |

---

### Phase 2: 改造 CardEffectExecutor ✅

#### 新增工厂方法

**1. GetTargetSelector(CardTargetType)**
```csharp
private ICardTargetSelector GetTargetSelector(CardTargetType targetType)
{
    return targetType switch
    {
        CardTargetType.Self => new SelfSelector(),
        CardTargetType.AllAllyExcludeSummoner => new AllAllyExcludeSummonerSelector(),
        CardTargetType.AllAlly => new AllAlliesSelector(),
        CardTargetType.AllEnemy => new AllEnemiesSelector(),
        CardTargetType.SingleAlly => new ClosestAllySelector(),
        CardTargetType.AreaAlly => new AlliesInRadiusSelector(),
        CardTargetType.AreaEnemy => new EnemiesInRadiusSelector(),
        CardTargetType.SingleEnemy => new ClosestEnemySelector(),
        _ => null
    };
}
```

**2. GetEffectAppliers(CardData)**
- 自动判断是否需要伤害应用器（基于 DamageType、DamageCoeff、BaseDamage）
- 自动判断是否需要治疗应用器（基于 ParamsConfig.healAmount）
- 自动判断是否需要 Buff 应用器（基于 HitBuffIds、InstantBuffIds）

```csharp
private ICardEffectApplier[] GetEffectAppliers(CardData cardData)
{
    var appliers = new List<ICardEffectApplier>();
    
    // 伤害应用器
    if (cardData.TableRow.DamageType > 0)
    {
        if (cardData.TableRow.DamageCoeff > 0)
            appliers.Add(new DamageWithCoefficientApplier(GetCasterChess()));
        else if (cardData.TableRow.BaseDamage > 0)
            appliers.Add(new DamageApplier());
    }
    
    // 治疗应用器
    if (cardData.GetParam("healAmount", 0f) > 0)
        appliers.Add(new HealApplier());
    
    // Buff 应用器
    if (cardData.HitBuffIds.Length > 0)
        appliers.Add(new BuffApplier());
    if (cardData.InstantBuffIds.Length > 0)
        appliers.Add(new InstantBuffApplier());
    
    return appliers.ToArray();
}
```

#### 简化 ExecuteCardEffect()

```csharp
public void ExecuteCardEffect(CardData cardData, Vector3 targetPosition)
{
    // 1. 尝试使用特殊脚本（复杂卡牌）
    if (m_EffectTypeMap.TryGetValue(cardData.CardId, out var effectType))
    {
        effectInstance = Activator.CreateInstance(effectType) as ICardEffect;
        effectInstance.Init(cardData);
    }
    // 2. 否则使用通用框架
    else
    {
        var targetSelector = GetTargetSelector(cardData.CTargetType);
        var appliers = GetEffectAppliers(cardData);
        effectInstance = CreateGenericEffect(cardData, targetSelector, appliers);
    }
    
    effectInstance.Execute(targetPosition);
}
```

**优势：**
- ❌ 移除了 1001-1011 的硬编码 case 分支（共 11 个）
- ✅ 一行代码通过配置表自动创建任何卡牌效果
- ✅ 新卡牌无需修改 C# 代码，只需配置 CardTable.txt

---

## 系统工作流

```
CardTable.txt（配置）
    ↓
CardData（解析：TargetType、DamageType、BaseDamage、HitBuffIds、InstantBuffIds、ParamsConfig）
    ↓
CardEffectExecutor.ExecuteCardEffect()
    ├─ 特殊卡（1003, 1006, 1012）→ m_EffectTypeMap → 自定义脚本
    └─ 通用卡（1001-1002, 1004-1005, 1007-1011, 1013-1025）
        ├─ GetTargetSelector(CTargetType) → ICardTargetSelector
        ├─ GetEffectAppliers(CardData) → ICardEffectApplier[]
        └─ GenericCardEffect → 选择目标 → 应用效果 → 播放特效
```

---

## 配置状态检查清单

### CardTable.txt ✅
- [x] 所有卡牌 1001-1027 已配置
- [x] TargetType 字段已正确填入（1-8 对应 8 种目标类型）
- [x] DamageType、BaseDamage、DamageCoeff 已配置
- [x] InstantBuffs、HitBuffs 已配置（Buff ID 列表）
- [x] ParamsConfig 已配置（JSON 参数）
- [x] EffectId、HitEffectId 已配置

### 新增 Buff（11 个）⏳
需要确认 BuffTable 是否已创建以下 Buff ID：
- 5011: 反伤Buff
- 5012: 护甲增强Buff
- 5013: 护盾再生Buff
- 5014: 驱散Buff
- 5015: 血量增强Buff
- 5016: 吸血Buff
- 5017: 灼烧Buff
- 5018: 破甲Buff
- 5019: 虚弱Buff
- 5020: 沉默Buff
- 5021: 嘲讽Buff
- 5022: 减速Buff
- 5023: 中毒Buff
- 5024: 冻结Buff

---

## Phase 3：配置确认 & 测试

### 需要用户操作

1. **确认 Buff 配置**
   - 检查 BuffTable.txt 中是否已有 Buff ID 5011-5024
   - 如果缺失，需要新增这些 Buff 定义

2. **运行 DataTableGenerator**
   ```
   GameFramework → DataTable → Generate
   ```
   这会生成/更新：
   - DRCardTable.cs（包含 1001-1027 卡牌的序列化数据）
   - DRBuffTable.cs（包含 Buff 定义）

3. **进入战斗测试每张卡牌**
   - 测试目标选择器是否正确（确保目标选中范围符合预期）
   - 测试效果应用器是否正确（伤害、治疗、Buff 是否生效）
   - 特别检查新选择器：
     - SelfSelector（自身选择）
     - AllAllyExcludeSummonerSelector（友方不含召唤师）
     - AlliesInRadiusSelector（范围友方）

4. **特殊卡牌**
   - 1003 时间回溯、1006 生命汲取、1012 不屈意志 - 继续用旧脚本（未迁移）
   - 1026 伤害转移、1027 绝对零度 - 暂未实现（需自定义脚本）

---

## 代码变更摘要

| 文件 | 变更 | 行数 |
|------|------|------|
| TargetSelectors.cs | +3 新选择器 | +70 |
| CardEffectExecutor.cs | +2 工厂方法，简化 ExecuteCardEffect | +50 |

**总计** 代码增加 ~120 行，删除 ~60 行硬编码 case 分支

---

## 架构优势总结

### 旧方案（硬编码 case）
```csharp
switch (cardData.CardId)
{
    case 1013:
        effectInstance = CreateGenericEffect(cardData,
            new ClosestAllySelector(),
            new InstantBuffApplier());
        break;
    case 1014:
        effectInstance = CreateGenericEffect(cardData,
            new AlliesInRadiusSelector(),  // 需要新增
            new InstantBuffApplier());
        break;
    // ... 重复 13 次
}
```
❌ 每张新卡需要修改 C# 代码  
❌ 硬编码选择器和应用器  
❌ 无法扩展（后续新卡仍需修改）

### 新方案（工厂模式）
```csharp
var targetSelector = GetTargetSelector(cardData.CTargetType);
var appliers = GetEffectAppliers(cardData);
effectInstance = CreateGenericEffect(cardData, targetSelector, appliers);
```
✅ 零硬编码 - 通过 CardTable 配置卡牌  
✅ 自动化 - GetEffectAppliers 智能判断应用器  
✅ 可扩展 - 新卡牌只需配置表，无需改代码  
✅ 可维护 - 所有逻辑集中在工厂方法

---

## 下一步

**待用户确认：**
1. Buff ID 5011-5024 是否已在 BuffTable 中定义？
2. 是否准备好运行 DataTableGenerator？
3. 是否需要测试实现？

确认后，可以：
- 运行 DataTableGenerator 更新数据表代码
- 进入战斗测试所有新卡牌效果
- 微调任何不符合预期的效果表现

> **最后更新**: 2026-04-17
> **状态**: 有效
> **分类**: 系统设计

---

# 敌人棋子数据重构

合并自：敌人棋子数据重构_设计方案 + 敌人棋子数据重构_完成总结

---

## 重构背景

**重构前问题**：
- 敌人棋子数据走 `GlobalChessManager`，与玩家棋子共用一套管理，数据互相污染
- 同一场景中两个同类型敌人（相同 ChessId）共享数据，HP 不独立
- 玩家战斗失败后敌人实体被销毁重建，无法保留当前 HP（每次都满血）

---

## 解决方案

为每个 `EnemyEntity` 分配 GUID，以 `"{guid}_{slotIndex}"` 为 key 在 `EnemyChessDataManager` 中独立存储每个棋子 HP 数据。战斗结束时按胜负决定是否销毁实体，HP 回写按 Camp 分发。

### 系统架构

```
探索场景
┌─────────────────────────────────────────────┐
│  EnemyEntity (guid=AAA)                     │
│  ├── 棋子槽0: ChessId=9, key="AAA_0"        │
│  └── 棋子槽1: ChessId=9, key="AAA_1"        │
│                                             │
│  EnemyEntity (guid=BBB) ← 同类型，独立数据   │
│  ├── 棋子槽0: ChessId=9, key="BBB_0"        │
│  └── 棋子槽1: ChessId=9, key="BBB_1"        │
└─────────────────────────────────────────────┘
            ↓ 战斗结束
EnemyChessDataManager（纯C#单例）
Dictionary<string, EnemyChessState>
"AAA_0" → {HP=87, MaxHP=100}
"AAA_1" → {HP=0,  MaxHP=100, IsDead=true}
            ↓ 战斗中回写
BattleChessManager
WriteBackHp: Camp==1 → EnemyChessDataManager
             Camp==0 → GlobalChessManager
```

### 核心组件

| 组件 | 职责 | 文件路径 |
|------|------|---------|
| `EnemyChessState` | 单个棋子 HP 数据（纯数据类） | `Game/Explore/Enemy/Core/EnemyChessState.cs` |
| `EnemyChessDataManager` | 全局管理所有敌人棋子数据，纯 C# 单例 | `Game/Explore/Enemy/Core/EnemyChessDataManager.cs` |
| `EnemyEntity` | 探索场景敌人，持有 GUID，初始化时注册棋子数据 | `Game/Explore/Enemy/Core/EnemyEntity.cs` |
| `EnemyEntityManager` | 管理敌人实体生命周期，战斗结束时按胜负分流 | `Game/Explore/Enemy/Core/EnemyEntityManager.cs` |
| `BattleChessData` | 扩展 Camp + EnemyKey 字段 | `Game/Combat/Core/BattleChessData.cs` |
| `BattleChessManager` | 战斗结束时按 Camp 回写 HP | `Game/Combat/Core/BattleChessManager.cs` |
| `EnemySpawnManager` | 生成棋子后从 EnemyChessDataManager 读取历史 HP | `Game/Combat/Enemy/EnemySpawnManager.cs` |
| `CombatPreparationState` | 进入战斗时隐藏（不销毁）敌人实体 | `GameState/States/CombatPreparationState.cs` |

---

## 关键设计决策

### 决策1：Register 跳过已有数据（HP 持久化核心）

```csharp
public void Register(string entityGuid, int slotIndex, int chessId, double maxHp)
{
    string key = BuildKey(entityGuid, slotIndex);
    if (m_Data.ContainsKey(key))
        return; // 已有历史数据，保留不覆盖
    m_Data[key] = new EnemyChessState(chessId, maxHp);
}
```

### 决策2：SetActive 代替 Destroy 来"隐藏"敌人

进入战斗时 `SetActive(false)`，失败后 `SetActive(true)` 恢复，保留所有组件状态。

**权衡**：SetActive(false/true) 会导致 NavMeshAgent 脱离 NavMesh，需要 `isOnNavMesh` 守卫（见探索系统Bug记录）。

### 决策3：CombatPreparationState 保留槽位顺序

不随机选取棋子，按配置顺序加载，`slotIndex` 与 EnemyChessDataManager 的 key 一一对应。随机顺序会导致 HP 数据匹配错误。

### 决策4：HP 回写按 Camp 分发

```csharp
private void WriteBackHp(int chessId, double hp, int camp)
{
    if (camp == 1)
    {
        if (m_BattleDataDict.TryGetValue(chessId, out var data) && !string.IsNullOrEmpty(data.EnemyKey))
            EnemyChessDataManager.Instance.UpdateHp(data.EnemyKey, hp);
    }
    else
    {
        GlobalChessManager.Instance.UpdateChessHP(chessId, hp);
    }
}
```

---

## 数据流

```
【初始化】
EnemyEntity.Awake() → 生成 GUID
EnemyEntity.Initialize() → 读 EnemyTable.ChessIds + SummonChessTable.MaxHp
  → EnemyChessDataManager.Register(guid, slotIndex, chessId, maxHp)（已有则跳过）

【进入战斗】
CombatTriggerManager.TriggerCombat()
  → HideEntityForCombat() → SetActive(false)
  → CombatPreparationState
  → EnemySpawnManager.LoadFromEnemyTable(battleConfigId, enemyGuid)
  → SpawnWaveAsync() → ApplyHistoricalHp() 继承 HP

【战斗中】
战斗结束 → BattleChessManager.OnBattleEnd() → WriteBackHp()

【战斗结束】
playerWin=true  → Destroy(entity.gameObject) → OnDestroy 清理 EnemyChessDataManager
playerWin=false → RestoreEntityAfterCombat() → SetActive(true) + ExitCombat()
两种情况均 → ResetAllEnemyAlertLevels()
```

---

## 修改的文件清单

| 文件 | 类型 |
|------|------|
| `EnemyChessState.cs` | 新建 |
| `EnemyChessDataManager.cs` | 新建 |
| `EnemyEntity.cs` | 修改 |
| `EnemyEntityManager.cs` | 修改 |
| `BattleChessData.cs` | 修改 |
| `BattleChessManager.cs` | 修改 |
| `EnemySpawnManager.cs` | 修改 |
| `CombatPreparationState.cs` | 修改 |
| `VisionConeDetector.cs` | 修改（新增 ResetAlert()） |
| `ChessLifecycleHandler.cs` | 修改（死亡棋子标记） |
| `EnemyIdleState.cs` | 修改（NavMesh守卫 + 战斗冷却） |
| `EnemyPatrolState.cs` | 修改（战斗冷却） |
| `EnemyCombatState.cs` | 修改（NavMesh守卫） |

---

## 已验证场景

- [x] 首次战斗：敌人棋子初始化为满 HP
- [x] 玩家失败：敌人实体恢复，HP 数据保留
- [x] 二次战斗：敌人棋子从上次结束 HP 开始
- [x] 玩家胜利：敌人消失，数据清理
- [x] 同场景两个同类型敌人：数据互相独立
- [x] 死亡棋子：出战界面显示 Mask，不可部署
- [x] 战败返回：不立即触发二次战斗（冷却 5 秒）

# 图4-2 系统整体架构与流程设计图

```dot
digraph SystemArchitecture {
    rankdir=LR;
    nodesep=0.5;
    ranksep=1.2;
    splines=true;
    fontname="SimHei,sans-serif";
    
    node [shape=box, style="filled,rounded", fontname="SimHei,sans-serif", fontsize=10, penwidth=1.5];
    edge [fontname="SimHei,sans-serif", fontsize=9, penwidth=1.2];
    
    // ========== 第1列：流程管理（黄色系） ==========
    subgraph cluster_procedure {
        label="游戏流程 (Procedure)";
        style="filled,rounded";
        fillcolor="#fffde7";
        color="#f57f17";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        PM [label="ProcedureManager\n(有限状态机)", fillcolor="#fff9c4", color="#f57f17"];
        P1 [label="LaunchProcedure\n启动流程", fillcolor="#fff9c4", color="#f57f17"];
        P2 [label="PreloadProcedure\n预加载流程", fillcolor="#fff9c4", color="#f57f17"];
        P3 [label="MenuProcedure\n主菜单流程", fillcolor="#fff9c4", color="#f57f17"];
        P4 [label="ExploreProcedure\n探索流程", fillcolor="#fff9c4", color="#f57f17"];
        P5 [label="CombatProcedure\n战斗流程", fillcolor="#fff9c4", color="#f57f17"];
    }
    
    // ========== 第2列：业务系统（橙色系） ==========
    subgraph cluster_business {
        label="业务系统 (Business)";
        style="filled,rounded";
        fillcolor="#fff3e0";
        color="#e65100";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        B1 [label="ExploreManager\n(探索系统)\n---\n敌人管理 / AI状态机\n战斗触发", fillcolor="#ffe0b2", color="#e65100"];
        B2 [label="CombatManager\n(战斗系统)\n---\n棋子AI / 技能执行\n伤害计算", fillcolor="#ffe0b2", color="#e65100"];
        B3 [label="ItemManager\n(物品系统)\n---\n背包管理 / 装备\n宝物词条", fillcolor="#ffe0b2", color="#e65100"];
        B4 [label="CardManager\n(卡牌系统)\n---\n卡牌管理\n效果执行", fillcolor="#ffe0b2", color="#e65100"];
        B5 [label="ConfigManager\n(配置系统)\n---\n配置数据\n访问接口封装", fillcolor="#ffccbc", color="#d84315"];
    }
    
    // ========== 第3列：事件系统（绿色系） ==========
    subgraph cluster_event {
        label="事件通信 (Event)";
        style="filled,rounded";
        fillcolor="#e8f5e9";
        color="#2e7d32";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        E1 [label="CombatStartEventArgs\n(战斗开始)", fillcolor="#c8e6c9", color="#2e7d32"];
        E2 [label="CombatEndEventArgs\n(战斗结束)", fillcolor="#c8e6c9", color="#2e7d32"];
        E3 [label="ItemObtainedEventArgs\n(物品获得)", fillcolor="#c8e6c9", color="#2e7d32"];
        E4 [label="EnemyPurifiedEventArgs\n(敌人净化)", fillcolor="#c8e6c9", color="#2e7d32"];
    }
    
    // ========== 第4列：数据层（紫色系） ==========
    subgraph cluster_data {
        label="数据层 (Data)";
        style="filled,rounded";
        fillcolor="#f3e5f5";
        color="#4a148c";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        D1 [label="ConfigManager\n(配置表缓存)\n---\nDataTable加载\n数据缓存", fillcolor="#e1bee7", color="#6a1b9a"];
        D2 [label="DataModel\n(运行时数据)\n---\n游戏状态\n玩家数据", fillcolor="#e1bee7", color="#6a1b9a"];
        D3 [label="UI数据绑定\n---\n数据驱动UI更新", fillcolor="#e1bee7", color="#6a1b9a"];
    }
    
    // ========== 流程管理内部 ==========
    PM -> P1 [label="启动"];
    P1 -> P2 [label="初始化完成"];
    P2 -> P3 [label="预加载完成"];
    P3 -> P4 [label="开始游戏"];
    P4 -> P5 [label="触发战斗"];
    P5 -> P4 [label="战斗结束返回", constraint=false];
    
    // ========== 流程 → 业务系统 ==========
    P4 -> B1 [label="驱动探索"];
    P5 -> B2 [label="驱动战斗"];
    
    // ========== 业务系统 → 事件 ==========
    B1 -> E1 [label="发起战斗"];
    B2 -> E2 [label="战斗结算"];
    B3 -> E3 [label="物品变更"];
    B1 -> E4 [label="净化敌人"];
    
    // ========== 事件 → 业务系统（订阅） ==========
    E1 -> B2 [label="触发战斗"];
    E2 -> B1 [label="通知探索恢复", constraint=false];
    E2 -> B3 [label="通知发放奖励"];
    E2 -> B4 [label="通知卡牌结算"];
    E4 -> B4 [label="触发卡牌获得"];
    
    // ========== 业务系统 → 数据层 ==========
    B5 -> D1 [label="封装配置访问"];
    B1 -> D2 [label="更新数据"];
    B2 -> D2 [label="更新数据"];
    B3 -> D2 [label="更新数据"];
    B4 -> D2 [label="更新数据"];
    D2 -> D3 [label="数据变更通知"];
    
    // ========== 业务系统 → 配置系统 ==========
    B1 -> B5 [label="读取配置", style=dashed];
    B2 -> B5 [label="读取配置", style=dashed];
    B3 -> B5 [label="读取配置", style=dashed];
    B4 -> B5 [label="读取配置", style=dashed];
    
    // ========== 列对齐 ==========
    {rank=same; PM; P1; P2; P3; P4; P5}
    {rank=same; B1; B2; B3; B4; B5}
    {rank=same; E1; E2; E3; E4}
    {rank=same; D1; D2; D3}
}
```

## 系统设计说明

### 1. 游戏流程管理

**ProcedureManager** 基于有限状态机（FSM）管理游戏的整体流程，控制游戏在以下流程之间切换：

#### 流程状态
- **LaunchProcedure** (启动流程)
  - 初始化游戏基础设置
  - 加载配置表和资源
  - 准备进入下一个流程

- **PreloadProcedure** (预加载流程)
  - 预加载必要的游戏资源
  - 初始化核心系统
  - 准备显示主菜单

- **MenuProcedure** (主菜单流程)
  - 显示主菜单UI
  - 等待玩家输入
  - 切换到游戏流程

- **ExploreProcedure** (探索流程)
  - 加载探索场景
  - 管理敌人实体和AI
  - 检测战斗触发条件
  - 可被打断进入战斗流程

- **CombatProcedure** (战斗流程)
  - 加载战斗场景
  - 管理棋子战斗
  - 战斗结束后返回探索流程

### 2. 业务系统协作

#### 系统交互流程

**探索 → 战斗**:
```
ExploreManager 检测到战斗条件
    ↓
发布 CombatStartEventArgs 事件
    ↓
ProcedureManager 切换到 CombatProcedure
    ↓
CombatManager 初始化战斗场景
```

**战斗 → 探索**:
```
CombatManager 战斗结束
    ↓
发布 CombatEndEventArgs 事件
    ↓
ItemManager 处理战斗奖励
CardManager 处理卡牌获得
EnemyManager 处理敌人净化
    ↓
ProcedureManager 切换回 ExploreProcedure
```

### 3. 事件系统设计

系统采用发布-订阅模式，通过事件进行模块间解耦通信：

- **CombatStartEventArgs**: 战斗开始事件
  - 触发者：探索系统（CombatTriggerManager）
  - 订阅者：战斗系统、UI系统

- **CombatEndEventArgs**: 战斗结束事件
  - 触发者：战斗系统
  - 订阅者：探索系统、物品系统、卡牌系统

- **ItemObtainedEventArgs**: 物品获得事件
  - 触发者：物品系统
  - 订阅者：UI系统

- **EnemyPurifiedEventArgs**: 敌人净化事件
  - 触发者：探索系统
  - 订阅者：卡牌系统、UI系统

### 4. 数据流向

**单向数据流设计**:
1. 配置数据：DataTable → ConfigManager → 各业务系统
2. 运行时数据：各业务系统 → DataModel（数据中心）
3. UI更新：DataModel → UI数据绑定 → 界面更新

**数据驱动架构**:
- 所有游戏参数通过配置表管理
- 业务系统通过配置系统访问数据
- 支持热更新和版本管理

### 5. 架构优势

1. **解耦**: 各系统通过事件通信，修改一个系统不影响其他系统
2. **可维护**: 清晰的流程流转和系统职责边界
3. **可扩展**: 新增功能只需实现系统接口，订阅相关事件
4. **性能**: 事件延迟触发、对象池复用、资源异步加载
5. **可测试**: 各系统相对独立，便于单元测试和集成测试

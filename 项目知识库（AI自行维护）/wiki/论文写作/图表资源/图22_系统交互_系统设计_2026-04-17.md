# 图22：系统交互示意图

```dot
digraph SystemInteraction {
    rankdir=TB;
    nodesep=0.8;
    ranksep=1.0;
    splines=orthogonal;
    
    User [label="用户操作\n(鼠标/键盘)", shape=box, fillcolor="#fff9c4", stroke="#f57f17", style=filled, penwidth=2];
    InputMgr [label="输入管理器\nPlayerInputManager", shape=box, fillcolor="#e3f2fd", stroke="#1976d2", style=filled, penwidth=2];
    EventSys [label="事件系统\nEventManager", shape=box, fillcolor="#f3e5f5", stroke="#4a148c", style=filled, penwidth=2];
    CombatSys [label="战斗系统\nCombatManager", shape=box, fillcolor="#e8f5e9", stroke="#1b5e20", style=filled, penwidth=2];
    BuffSys [label="Buff系统\nBuffManager", shape=box, fillcolor="#fce4ec", stroke="#c2185b", style=filled, penwidth=2];
    UISYS [label="UI系统\nUIManager", shape=box, fillcolor="#fff3e0", stroke="#e65100", style=filled, penwidth=2];
    GameState [label="游戏状态\nGameState", shape=box, fillcolor="#e0f2f1", stroke="#00695c", style=filled, penwidth=2];
    
    User -> InputMgr [label="输入事件"];
    InputMgr -> EventSys [label="解析输入"];
    EventSys -> CombatSys [label="发布事件"];
    EventSys -> UISYS [label="发布事件"];
    CombatSys -> BuffSys [label="应用效果"];
    BuffSys -> GameState [label="通知系统"];
    CombatSys -> UISYS [label="更新显示"];
    UISYS -> User [label="刷新界面"];
    GameState -> UISYS [label="状态变化"];
}
```

## 系统交互设计说明

### 交互流程

**1. 用户输入阶段**
- 用户通过鼠标或键盘进行操作
- 所有输入必须走 PlayerInputManager

**2. 输入管理与事件发布**
- InputManager 解析原始输入
- EventManager 负责事件分发
- 采用发布-订阅模式

**3. 系统响应**
- 各系统独立订阅所需事件
- CombatSystem 处理战斗逻辑
- BuffSystem 应用效果状态
- UIManager 更新用户界面

**4. 状态更新与反馈**
- GameState 维护全局状态
- UI 实时反映当前状态
- 用户获得视觉反馈


# 战斗触发与脱战机制架构图

```dot
digraph CombatTriggerAndEscape {
    rankdir=LR;
    nodesep=0.5;
    ranksep=1.2;
    splines=ortho;
    fontname="SimHei,sans-serif";
    
    node [shape=box, style="filled,rounded", fontname="SimHei,sans-serif", fontsize=10, penwidth=1.5];
    edge [fontname="SimHei,sans-serif", fontsize=9, penwidth=1.2];
    
    // ========== 第1列：探索层（绿色系） ==========
    subgraph cluster_explore {
        label="探索阶段";
        style="filled,rounded";
        fillcolor="#e8f5e9";
        color="#2e7d32";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        ExplorationState [label="ExplorationState\n(探索状态)", fillcolor="#c8e6c9", color="#2e7d32"];
        Detector [label="CombatOpportunityDetector\n(战斗机会检测器)", fillcolor="#c8e6c9", color="#2e7d32"];
        EnemyEntity [label="EnemyEntity\n(敌人实体)", fillcolor="#a5d6a7", color="#1b5e20"];
        PlayerInput [label="PlayerInputManager\n(空格键触发)", fillcolor="#a5d6a7", color="#1b5e20"];
    }
    
    // ========== 第2列：触发层（橙色系） ==========
    subgraph cluster_trigger {
        label="触发判定";
        style="filled,rounded";
        fillcolor="#fff3e0";
        color="#e65100";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        TriggerMgr [label="CombatTriggerManager\n(战斗触发管理器)\n---\nTriggerCombat()\nClearContext()", fillcolor="#ffe0b2", color="#e65100"];
        TriggerCtx [label="CombatTriggerContext\n(触发上下文)\n---\nTriggerType\nAvailableDebuffs\nInitiativeBuffId", fillcolor="#ffccbc", color="#d84315"];
        
        subgraph cluster_types {
            label="CombatTriggerType";
            style="filled,rounded";
            fillcolor="#fff9c4";
            color="#f57f17";
            fontname="SimHei,sans-serif";
            fontsize=10;
            
            TypeSneak [label="SneakAttack\n偷袭\n距离\<3m 背后\n警觉\<0.3", fillcolor="#fff176", color="#f57f17", fontsize=8];
            TypeEncounter [label="Encounter\n遭遇战\n距离\<5m 正面\n警觉\<0.5", fillcolor="#fff176", color="#f57f17", fontsize=8];
            TypeEnemy [label="EnemyInitiated\n敌方先手\n敌人追击触发", fillcolor="#fff176", color="#f57f17", fontsize=8];
            TypeNormal [label="Normal\n普通触发", fillcolor="#fff176", color="#f57f17", fontsize=8];
        }
    }
    
    // ========== 第3列：战斗层（蓝色系） ==========
    subgraph cluster_combat {
        label="战斗阶段";
        style="filled,rounded";
        fillcolor="#e3f2fd";
        color="#1565c0";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        PrepState [label="CombatPreparationState\n(战斗准备状态)\n---\n记录玩家位置\n显示入场提示\n生成敌人棋子", fillcolor="#bbdefb", color="#1565c0"];
        CombatState [label="CombatState\n(战斗状态)\n---\n启用战斗系统\n订阅结束事件\n启用棋子AI", fillcolor="#90caf9", color="#0d47a1"];
        CombatMgr [label="CombatManager\n(战斗管理器)\n---\nStartCombat()\nEndCombat(isVictory)\nIsInCombat", fillcolor="#bbdefb", color="#1565c0"];
        Tracker [label="CombatEntityTracker\n(实体追踪器)\n---\n按阵营分组棋子\n构建敌人缓存", fillcolor="#bbdefb", color="#1565c0"];
        BattleChess [label="BattleChessManager\n(棋子管理器)\n---\nOnBattleEnd()\n回写血量到全局", fillcolor="#bbdefb", color="#1565c0"];
    }
    
    // ========== 第4列：结束层（紫/红色系） ==========
    subgraph cluster_end {
        label="结束/脱战";
        style="filled,rounded";
        fillcolor="#fce4ec";
        color="#c2185b";
        fontname="SimHei,sans-serif";
        fontsize=12;
        
        EscapeSys [label="CombatEscapeSystem\n(脱战系统)\n---\nAttemptEscape()\nCalculateSuccessRate()\n基础率+回合奖励\n冷却期减半", fillcolor="#f8bbd0", color="#c2185b"];
        EscapeOK [label="脱战成功\n---\n增加污染值\n清除上下文", fillcolor="#e1bee7", color="#6a1b9a"];
        EscapeFail [label="脱战失败\n---\n设置冷却回合", fillcolor="#ffcdd2", color="#b71c1c"];
        
        CombatEnd [label="CombatEndEventArgs\n(战斗结束事件)\n---\nIsVictory", fillcolor="#f8bbd0", color="#c2185b"];
        EnemyMgr [label="EnemyEntityManager\n(敌人管理器)\n---\nTriggerCombat()\nOnCombatEnd()", fillcolor="#f8bbd0", color="#c2185b"];
    }
    
    // 返回探索节点
    ReturnExplore [label="返回探索状态\n---\n恢复玩家位置\n恢复相机视角\n销毁战斗场地", fillcolor="#c8e6c9", color="#2e7d32"];
    
    // ========== 主流程边 ==========
    
    // 探索 → 检测
    ExplorationState -> Detector [label="Update检测"];
    PlayerInput -> Detector [label="空格键"];
    EnemyEntity -> EnemyMgr [label="追击触发", style=dashed];
    
    // 检测 → 触发
    Detector -> TriggerMgr [label="触发战斗"];
    Detector -> TypeSneak [label="偷袭条件满足", style=dashed];
    Detector -> TypeEncounter [label="遭遇条件满足", style=dashed];
    EnemyMgr -> TriggerMgr [label="敌方先手触发"];
    
    // 触发类型 → 上下文
    TypeSneak -> TriggerCtx [label="分配Debuff池", constraint=false];
    TypeEncounter -> TriggerCtx [label="随机玩家Buff", constraint=false];
    TypeEnemy -> TriggerCtx [label="随机敌方Buff", constraint=false];
    TypeNormal -> TriggerCtx [constraint=false];
    TriggerMgr -> TriggerCtx [label="创建上下文"];
    
    // 触发 → 准备 → 战斗
    TriggerMgr -> EnemyMgr [label="保存战斗数据"];
    EnemyMgr -> PrepState [label="切换状态"];
    PrepState -> CombatState [label="玩家确认"];
    
    // 战斗内部
    CombatState -> CombatMgr [label="EnableCombatManager"];
    CombatMgr -> Tracker [label="BuildEnemyCache"];
    CombatMgr -> BattleChess [label="管理棋子"];
    
    // 脱战分支
    CombatState -> EscapeSys [label="玩家选择脱战"];
    EscapeSys -> EscapeOK [label="成功"];
    EscapeSys -> EscapeFail [label="失败"];
    EscapeFail -> CombatState [label="继续战斗", constraint=false];
    
    // 战斗结束
    CombatMgr -> CombatEnd [label="EndCombat()"];
    EscapeOK -> CombatEnd [label="触发结束"];
    CombatEnd -> EnemyMgr [label="通知胜负", constraint=false];
    
    // 返回探索
    CombatEnd -> ReturnExplore [label="CombatState.OnLeave()"];
    ReturnExplore -> ExplorationState [label="状态切换", constraint=false];
    
    // ========== 行对齐 ==========
    {rank=same; ExplorationState; Detector; EnemyEntity; PlayerInput}
    {rank=same; TriggerMgr; TriggerCtx}
    {rank=same; PrepState; CombatState; CombatMgr}
    {rank=same; EscapeSys; CombatEnd; EnemyMgr}
    {rank=same; EscapeOK; EscapeFail; ReturnExplore}
}
```

## 架构说明

### 四大阶段

| 阶段 | 核心组件 | 职责 |
|------|---------|------|
| 探索阶段 | ExplorationState, CombatOpportunityDetector | 检测偷袭/遭遇战机会，等待玩家空格触发 |
| 触发判定 | CombatTriggerManager, CombatTriggerContext | 判断触发类型，分配先手Buff/偷袭Debuff |
| 战斗阶段 | CombatState, CombatManager, CombatEntityTracker | 管理战斗全生命周期，追踪所有棋子 |
| 结束/脱战 | CombatEscapeSystem, CombatEndEventArgs | 脱战成功率计算，战斗结果处理 |

### 触发类型

| 类型 | 条件 | 效果 |
|------|------|------|
| SneakAttack（偷袭） | 距离<3m, 背后, 警觉<0.3 | 给敌人施加Debuff池 |
| Encounter（遭遇战） | 距离<5m, 正面, 警觉<0.5 | 随机获得玩家先手Buff |
| EnemyInitiated（敌方先手） | 敌人追击到达 | 随机敌方获得先手Buff |
| Normal（普通） | 默认 | 无额外效果 |

### 脱战机制

- **成功率** = 基础率 + 回合数 × 时间奖励，上限封顶
- **冷却期**：失败后N回合内成功率减半
- **代价**：成功脱战增加污染值
- **失败**：设置冷却回合数，继续战斗

### 关键事件流

```
CombatEndEventArgs  → CombatState.OnCombatEnd() → 更新污染值（失败时）
                    → EnemyEntityManager.OnCombatEnd() → 胜利销毁敌人/失败恢复敌人
CombatLeaveEventArgs → ChessStateUIWorldManager → 清理战斗UI
```

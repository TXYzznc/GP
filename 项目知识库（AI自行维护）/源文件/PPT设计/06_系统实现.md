# 第5章：系统实现

> 对应论文第5章「系统实现」
> 预计10-12页，演讲时间4-5分钟（**重点章节**）
> 此章节以实际截图和效果展示为主，辅以关键代码

---

## 页面5-1：章节导航页

### 布局
- 复用目录页，第⑤项「系统实现」高亮
- 右下角标注"核心章节"

---

## 页面5-2：技术美术 — 着色器效果

### 标题
`技术美术实现 — 自定义着色器`

### 内容布局：三列对比展示

**列1：角色描边着色器**
- 展示图：角色描边效果对比图（论文图5.1）
- 说明文字：
  - 两Pass方案
  - 法线外扩描边
  - 支持运行时动态调整
  - 用于棋子选中高亮

**列2：溶解着色器**
- 展示图：溶解过渡效果三帧图（论文图5.2）
- 说明文字：
  - 噪声贴图采样
  - 边缘发光效果
  - DOTween驱动过渡
  - 用于角色死亡/场景转换

**列3：卡通油画渲染**
- 展示图：三种渲染风格对比图（论文图5.3）
- 说明文字：
  - 阶梯式光照模型
  - 三区域分色（亮/过渡/暗）
  - 边缘光增强立体感
  - 统一视觉风格

### 底部标注
- 渲染管线：URP (Universal Render Pipeline)
- 关键优化：SRP Batcher、GPU Instancing

---

## 页面5-3：战斗系统实现 — 核心流程

### 标题
`战斗系统实现 — 流程与AI`

### 内容布局：上下分区

**上半部分：战斗流程状态图**
```
[探索触发] → [战斗准备] → [先手效果] → [自动战斗] → [结算/奖励]
                │                          │
                ├─ 布置棋子               ├─ AI每帧Tick
                ├─ 装备预览               ├─ 技能冷却
                └─ 倒计时                 ├─ Buff刷新
                                          └─ 死亡检测
```

**下半部分：关键代码片段（AI攻击缓存机制）**
```csharp
// AI决策 - 攻击缓存机制
private void TryAttack()
{
    m_CachedTarget = FindNearestEnemy();  // 缓存目标
    m_CachedDamage = CalculateDamage();  // 预计算伤害
    m_Controller.PlayAttack();            // 触发动画
}

// 动画事件回调 - 从缓存读取
void AnimEvent_AttackExecute()
{
    if (m_CachedTarget != null && m_CachedTarget.IsAlive)
        m_NormalAttack.Execute(m_CachedTarget, m_CachedDamage);
}
```

### 标注
- 攻击缓存避免动画期间目标状态变化的问题
- UniTask 管理异步战斗流程
- 全程事件驱动，模块间无直接调用

---

## 页面5-4：战斗系统实现 — 战斗界面展示

### 标题
`战斗系统实现 — 实际效果`

### 内容布局：大截图+标注

**主图**：战斗场景截图（论文图5.15）— 占页面70%

**标注箭头指向**：
1. 棋子血条（实时更新，颜色渐变）
2. Buff图标列表（动态增减）
3. 伤害飘字（暴击特殊颜色）
4. 策略卡手牌区（扇形排列）
5. 召唤师状态条（HP/MP）
6. 脱战/刷新按钮

**底部数据卡片**：
| 指标 | 数值 |
|------|------|
| 支持棋子数 | 最多8v8 |
| Buff类型 | 50+ |
| 命中检测方式 | 5种 |
| AI类型 | 3种（近战/远程/辅助）|

---

## 页面5-5：物品系统实现

### 标题
`物品 & 背包系统实现`

### 内容布局：左右双截图+中间说明

**左侧截图**：背包界面（论文图5.7）
- 标注：网格布局、品质边框颜色、堆叠数量显示

**中间说明**：
- AddItem 操作逻辑：
  1. 检查可否堆叠
  2. 尝试合并到已有格子
  3. 找空格子创建新堆叠
  4. 满则触发事件提示
- 排序规则：类型 → 品质 → 空格后置

**右侧截图**：物品详情面板（论文图5.5/5.6）
- 标注：物品名称、描述、属性加成、词条列表

**底部截图**：拖拽交互效果（论文图5.8）
- 标注：半透明原格、拖拽图标跟随鼠标、目标高亮

---

## 页面5-6：卡牌系统实现

### 标题
`策略卡系统实现`

### 内容布局：上截图+下流程

**上半部分：卡牌UI截图（论文图5.9）**
- 扇形排列的手牌展示
- 标注：选中放大效果、卡牌详情面板

**下半部分：卡牌效果执行流程**
```
玩家拖拽卡牌到战场
        │
        ▼
   检查灵力是否充足
        │ ✓
        ▼
   扣除灵力消耗
        │
        ▼
   CardEffectExecutor 分发
        │
   ┌────┼────────────────┐
   │    │                │
   ▼    ▼                ▼
 通用效果框架      特殊效果类
 (目标选择器+      (生命汲取
  效果应用器)       不屈意志等)
        │                │
        ▼                ▼
  CardEffectHelper 统一接口
  (DealDamage/HealTarget/ApplyBuff/PlayEffect)
        │
        ▼
   从手牌移除卡牌
```

**右侧数据**：
- 已实现12种卡牌效果
- 8种目标选择类型
- 支持即时Buff和命中时Buff

---

## 页面5-7：探索系统实现

### 标题
`探索系统实现 — AI状态机 & 视野检测`

### 内容布局：左右分栏

**左栏：探索场景截图**（如果有，展示玩家在探索场景中的截图）
- 标注敌人巡逻路径
- 标注视野检测范围示意（半透明扇形/圆形叠加）
- 标注战斗触发提示UI

**右栏：实现要点**

**AI状态机实现**：
- 6状态：Idle / Patrol / Alert / Chase / Combat / Death
- 状态对象模式（每状态一个类）
- NavMeshAgent驱动移动
- DebugEx日志追踪状态切换

**视野检测实现**：
- 圆形感知：`Physics.OverlapSphereNonAlloc`
- 扇形视觉：向量夹角计算
- 警觉度：[0,1]连续值，0.15s更新周期
- 三种屏蔽机制防止误触发

**战斗触发类型**：
| 类型 | 条件 | 效果 |
|------|------|------|
| 偷袭 | 背后+低警觉+近距离 | 玩家选Debuff施加敌人 |
| 遭遇战 | 正面+低警觉 | 玩家获得先手Buff |
| 敌方先手 | 被追上 | 敌人获得先手Buff |

---

## 页面5-8：UI系统实现 — 主要界面展示

### 标题
`UI系统实现 — 界面一览`

### 内容布局：4-6张UI截图网格

| 位置 | 截图 | 说明 |
|------|------|------|
| 左上 | 主菜单界面（图5.12） | DOTween Sequence多层入场动画 |
| 右上 | 战前准备界面（图5.13） | 棋子手牌+装备预览+Buff选择 |
| 左中 | 战斗界面（图5.15） | 血条+Buff图标+手牌+飘字 |
| 右中 | 战斗结算界面（图5.16） | 宝箱开启+奖励逐件弹出 |
| 左下 | 世界地图界面（图5.17） | 场景解锁+传送系统 |
| 右下 | Buff选择界面（图5.14） | 三选一+错落弹出动效 |

### 底部总结表

| UI模块 | 异步方案 | 动画方案 | 对象池 |
|--------|---------|---------|--------|
| 主菜单 | — | DOTween Sequence | — |
| 战前准备 | UniTask+CTS | 错落弹出 | 棋子卡 |
| 战斗UI | UniTask.Yield | 扇形进场 | 卡牌 |
| 结算UI | UniTask+CTS | OutBack弹出 | — |

---

## 页面5-9：性能优化策略

### 标题
`性能优化策略`

### 内容布局：三列展示（CPU/GPU/内存）

**列1：CPU优化**
- 🔄 对象池：特效、UI Item、投射物
- ⏱️ 频率控制：视野0.15s、触发0.1s
- 📝 字符串优化：条件编译限制日志
- ⚡ NonAlloc物理检测：避免GC

**列2：GPU优化**
- 📦 SRP Batcher：减少CPU渲染开销
- 👥 GPU Instancing：同模型共享DrawCall
- 🏔️ LOD系统：远距离低精度模型
- 💡 预烘焙光照：替代实时计算
- 展示图：光照预烘焙示例（图5.10）

**列3：内存优化**
- 📐 异步资源加载：避免主线程阻塞
- 🗜️ 纹理压缩：DXT5/BC7 节省50-70%
- 🎨 Sprite Atlas：图标打包减少DrawCall
- 🗑️ 及时卸载：UnloadAsset防泄漏
- 展示图：图集打包示例（图5.11）

---

## 页面5-10：数据配置系统实现

### 标题
`配置表系统实现`

### 内容布局：上代码+下截图

**上半部分：关键代码展示**

```csharp
// 并行异步加载所有配置表
private async UniTask LoadDataTablesAsync()
{
    var tableNames = AppConfigs.Instance.DataTableNames;
    var tasks = tableNames.Select(name =>
        GF.DataTable.LoadDataTableAsync(name, useBytes: true));
    await UniTask.WhenAll(tasks);
}

// 静态缓存机制 - O(1)查询
public static DRBuffTable GetCached(int id)
{
    if (!s_Cache.TryGetValue(id, out var row))
    {
        row = GF.DataTable.GetDataTable<BuffTable>().GetDataRow(id);
        if (row != null) s_Cache[id] = row;
    }
    return row;
}
```

**下半部分：BuffTable配置表截图（论文图4.18）**
- 展示Excel编辑界面
- 标注：第1行注释、第2行字段名、第3行类型、第4行起数据

**右侧关键数据**：
- 配置表总数：50张
- 加载方式：并行异步
- 缓存命中率：接近100%（首次Miss后缓存）
- 生成工具：DataTableGenerator 一键生成 .cs + .bytes

---

## 页面5-11：动画系统实现

### 标题
`动画系统 — 事件驱动执行`

### 内容布局：上图+下说明

**上半部分：动画事件帧配置截图（论文图5.4）**
- 展示Unity Animator窗口中Attack动画的事件帧
- 标注AnimEvent_AttackExecute的位置（约40%-60%帧）

**下半部分：动画驱动执行链**
```
Animator State Machine
    │
    ├── Idle (BlendTree: Speed参数混合)
    ├── Move (BlendTree)
    ├── Attack ──→ [AnimEvent_AttackExecute] ──→ 触发技能执行
    ├── Skill1 ──→ [AnimEvent_SkillExecute]  ──→ 触发技能执行
    ├── Hit
    └── Death
```

**关键设计点**：
- 动画事件确保伤害判定与动画视觉同步
- 避免伤害提前/延迟触发
- 混合树实现平滑的移动过渡
- 7个动画状态覆盖棋子全部行为

---

## 页面5-12：实现成果总结

### 标题
`系统实现成果总结`

### 内容布局：大数字 + 关键成果矩阵

**顶部大数字展示**（类似Dashboard风格）：
```
┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│  12个  │  │  50张  │  │  50+   │  │  52项  │
│核心系统│  │配置表  │  │Buff类型│  │测试用例│
└────────┘  └────────┘  └────────┘  └────────┘
```

**下方关键成果列表**：
| 系统模块 | 核心实现 | 关键技术 |
|---------|---------|---------|
| 战斗系统 | 自走棋机制、Buff、技能、AI | UniTask、工厂模式、策略模式 |
| 物品系统 | 50槽背包、拖拽交互、词条 | 对象池、状态机、事件驱动 |
| 卡牌系统 | 12种效果、效果引擎 | 命令模式、工厂字典 |
| 探索系统 | 6状态FSM、视野检测 | NavMesh、物理检测 |
| 技术美术 | 3套着色器、粒子特效 | URP、ShaderGraph |
| 配置系统 | 50表并行加载、缓存 | 异步加载、泛型接口 |
| 性能优化 | 对象池、LOD、GPU Instancing | SRP Batcher、纹理压缩 |

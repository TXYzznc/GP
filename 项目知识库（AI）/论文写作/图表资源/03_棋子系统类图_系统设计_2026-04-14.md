# 棋子系统类图

```dot
digraph ChessSystem {
    rankdir=LR;
    nodesep=0.6;
    ranksep=1.0;
    splines=orthogonal;
    
    node [shape=box, style=filled, fontsize=10];
    edge [arrowhead=vee];
    
    ChessEntity [label="{ChessEntity|+ m_AI: ChessAI\n+ m_Combat: CombatController\n+ m_NormalAttack: IChessSkill\n+ m_Transform: Transform\n+ m_BuffManager: BuffManager\n+ m_Attributes: ChessAttributes\n---\n+ Initialize()\n+ Tick()\n+ TakeDamage(damage, isCritical)\n+ Die()\n+ GetAttribute(attrType): double}", shape=record, fillcolor="#e3f2fd", color="#1976d2", penwidth=2];
    
    ChessAI [label="{ChessAI|+ m_Entity: ChessEntity\n+ m_IsInCombat: bool\n+ m_CurrentTarget: ChessEntity\n+ m_State: AIState\n---\n+ FindTarget(): ChessEntity\n+ Attack(target)\n+ Tick()\n+ ChangeState(newState)}", shape=record, fillcolor="#e3f2fd", color="#1976d2", penwidth=2];
    
    CombatController [label="{CombatController|+ m_Entity: ChessEntity\n+ m_CurrentSkill: IChessSkill\n+ m_SkillExecutor: SkillExecutor\n---\n+ OnAttackExecute(skillId)\n+ OnSkillExecute(skillId)\n+ OnSkillEnd()}", shape=record, fillcolor="#e3f2fd", color="#1976d2", penwidth=2];
    
    ChessAttributes [label="{ChessAttributes|+ HP: double\n+ MaxHP: double\n+ ATK: double\n+ DEF: double\n+ CritRate: float\n+ CritDamage: float\n+ Speed: float\n---\n+ GetFinalValue(attrType): double\n+ ApplyModifier(modifier)\n+ RemoveModifier(modifier)}", shape=record, fillcolor="#e3f2fd", color="#1976d2", penwidth=2];
    
    IChessSkill [label="{IChessSkill|+ skillId: int\n+ skillConfig: SkillConfig\n---\n+ ExecuteAttack(caster, target)*\n+ CalculateDamage(caster)*\n+ GetSkillConfig()*\n+ IsAvailable()*}", shape=record, fillcolor="#fff3e0", color="#e65100", penwidth=2];
    
    IHitDetector [label="{IHitDetector|---\n+ Execute(context)*}", shape=record, fillcolor="#fff3e0", color="#e65100", penwidth=2];
    
    ChessNormalAttackBase [label="{ChessNormalAttackBase|# m_Config: SkillConfig\n# m_LastExecuteTime: float\n---\n+ ExecuteAttack(caster, target)\n+ CalculateDamage(caster, isCritical)\n+ GetSkillConfig()\n+ GetCooldownTime()}", shape=record, fillcolor="#bbdefb", color="#1565c0", penwidth=2];
    
    HitDetectorBase [label="{HitDetectorBase|# m_Context: HitContext\n---\n+ Execute(context)\n# ApplyDamage(context)\n# ApplyBuffsOnHit()}", shape=record, fillcolor="#bbdefb", color="#1565c0", penwidth=2];
    
    InstantDetector [label="{InstantDetector|---\n+ Execute(context)}", shape=record, fillcolor="#c8e6c9", color="#1b5e20", penwidth=2];
    
    MeleeDetector [label="{MeleeDetector|+ meleeRange: float\n---\n+ Execute(context)}", shape=record, fillcolor="#c8e6c9", color="#1b5e20", penwidth=2];
    
    ProjectileDetector [label="{ProjectileDetector|+ projectileSpeed: float\n+ projectilePrefab: string\n---\n+ Execute(context)}", shape=record, fillcolor="#c8e6c9", color="#1b5e20", penwidth=2];
    
    HitDetectorFactory [label="{HitDetectorFactory|---\n+ GetDetector(type): IHitDetector}", shape=record, fillcolor="#f1f8e9", color="#558b2f", penwidth=2];
    
    HitContext [label="{HitContext|+ Attacker: ChessEntity\n+ LockedTarget: ChessEntity\n+ BaseDamage: double\n+ IsCritical: bool\n+ SkillConfig: SkillConfig\n+ OnHitCallback: Action}", shape=record, fillcolor="#fce4ec", color="#c2185b", penwidth=2];
    
    SkillConfig [label="{SkillConfig|+ skillId: int\n+ skillName: string\n+ baseDamage: float\n+ damageType: int\n+ cooldownTime: float\n+ targetType: int\n+ buffIds: int[]\n+ selfBuffIds: int[]}", shape=record, fillcolor="#fff3e0", color="#e65100", penwidth=2];
    
    BuffManager [label="{BuffManager|+ m_ActiveBuffs: BaseBuff[]\n---\n+ AddBuff(buffId, caster)\n+ RemoveBuff(buffId)\n+ HasBuff(buffId): bool\n+ Tick(deltaTime)}", shape=record, fillcolor="#f3e5f5", color="#4a148c", penwidth=2];
    
    // 组合关系（ChessEntity 持有）
    ChessEntity -> ChessAI [label="持有AI", arrowhead=diamond];
    ChessEntity -> CombatController [label="持有战斗控制", arrowhead=diamond];
    ChessEntity -> ChessAttributes [label="持有属性", arrowhead=diamond];
    ChessEntity -> BuffManager [label="持有Buff管理", arrowhead=diamond];
    ChessEntity -> IChessSkill [label="持有普攻"];
    
    // ChessAI 关系
    ChessAI -> ChessEntity [label="引用目标", style=dashed, constraint=false];
    
    // CombatController 关系
    CombatController -> IChessSkill [label="执行技能"];
    
    // 继承/实现关系
    ChessNormalAttackBase -> IChessSkill [arrowhead=empty, label="实现"];
    HitDetectorBase -> IHitDetector [arrowhead=empty, label="实现"];
    InstantDetector -> HitDetectorBase [arrowhead=empty, label="继承"];
    MeleeDetector -> HitDetectorBase [arrowhead=empty, label="继承"];
    ProjectileDetector -> HitDetectorBase [arrowhead=empty, label="继承"];
    
    // 依赖关系
    ChessNormalAttackBase -> HitDetectorFactory [label="获取检测器", style=dashed];
    ChessNormalAttackBase -> SkillConfig [label="引用配置", style=dashed];
    HitDetectorFactory -> IHitDetector [label="创建", style=dashed];
    HitDetectorBase -> HitContext [label="使用上下文"];
    IChessSkill -> SkillConfig [label="持有配置", arrowhead=diamond];
    BuffManager -> ChessAttributes [label="修改属性", style=dashed];
}
```

## 类设计说明

### 核心实体

**ChessEntity** (棋子实体)
- 棋子系统的核心类，代表场上一个战斗单位
- 持有AI、战斗控制器、属性、Buff管理器
- 提供初始化、受击、死亡等生命周期接口

**ChessAI** (棋子AI)
- 控制棋子的自动行为
- 寻找目标、发起攻击
- 支持状态切换

**CombatController** (战斗控制器)
- 协调技能执行流程
- 处理攻击和技能事件
- 管理当前执行的技能

**ChessAttributes** (棋子属性)
- 管理棋子的所有战斗属性
- 支持Buff修正器的叠加和移除
- 提供最终属性值计算

### 接口与抽象类

**IChessSkill** (技能接口)
- 所有技能的统一接口
- 支持多态实现不同技能类型

**IHitDetector** (命中检测接口)
- 定义命中检测的统一接口

**ChessNormalAttackBase** (普攻基类)
- 实现IChessSkill接口
- 提供普通攻击的通用逻辑

**HitDetectorBase** (命中检测基类)
- 实现IHitDetector接口
- 提供伤害应用和Buff触发的通用逻辑

### 具体实现

- **InstantDetector**: 即时命中检测（法术直接命中）
- **MeleeDetector**: 近战命中检测（带距离判定）
- **ProjectileDetector**: 弹道命中检测（投射物飞行）

### 辅助类

**HitDetectorFactory** (检测器工厂)
- 根据类型创建对应的命中检测器

**HitContext** (命中上下文)
- 传递命中检测所需的所有信息

**SkillConfig** (技能配置)
- 来自SkillTable的技能配置数据

**BuffManager** (Buff管理器)
- 管理棋子身上的所有Buff效果
- 通过修改ChessAttributes影响战斗

## 关键设计特点

1. **组合模式**: ChessEntity 通过组合持有各子系统
2. **多态技能**: IChessSkill 接口支持不同职业技能实现
3. **策略模式**: HitDetector 按类型选择不同命中检测策略
4. **工厂模式**: HitDetectorFactory 统一创建检测器
5. **配置驱动**: 技能参数全部来自配置表
6. **属性修正**: BuffManager 通过修正器影响 ChessAttributes

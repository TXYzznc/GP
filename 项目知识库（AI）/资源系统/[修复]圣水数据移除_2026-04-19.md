# 圣水数据移除修复记录

**日期**: 2026-04-19  
**状态**: 完成（需要运行 DataTableGenerator）

---

## 修改概述

根据新的资源管理设计，圣水从**全局资源**改为**战斗资源**（局内临时），因此需要从玩家账号数据管理中移除圣水的全局数据存储。

---

## 已修改的文件

### 1. ✅ `Assets/AAAGame/Scripts/DataModel/PlayerAccountData.cs`
**修改内容**：
- 删除了 `public int HolyWater;` 字段（第 99 行）
- 圣水不再作为全局资源存储

**原因**：圣水是战斗资源，每局临时，不需要全局保存

---

### 2. ✅ `Assets/AAAGame/Scripts/UI/CurrencyUI.cs`
**修改内容**：
- 删除了 `CreateCurrencyItem(1103, saveData.HolyWater);` 行
- 更新注释：从"创建三种货币"改为"创建两种货币"
- 现在只显示：金币（1101）和起源石（1102）

**原因**：圣水不再是全局资源，不需要在货币UI中显示

---

### 3. ✅ `Assets/AAAGame/Scripts/Manager/PlayerAccountDataManager.cs`
**修改内容**：
- 删除了初始化代码：`HolyWater = initConfig.InitHolyWater,`（第 311 行）
- 删除了方法：`public void AddHolyWater(int amount)` 
- 删除了方法：`public bool ConsumeHolyWater(int amount)`

**原因**：不再需要全局管理圣水的增减

---

### 4. ✅ `Assets/Editor/unity开发集成工具箱/Panels/AccountDataEditorPanel.cs`
**修改内容**：
- 删除了编辑器中的圣水字段编辑行
- 更新标签：从"灵石"改为"起源石"
- 现在编辑器只显示：金币和起源石

**原因**：编辑器不再需要编辑圣水数据

---

## 需要手动完成的操作

### ⚠️ 重要：运行 DataTableGenerator

以下文件是**自动生成**的，需要重新运行 DataTableGenerator 工具来更新：

1. `Assets/AAAGame/Scripts/DataTable/PlayerAccountTable.cs`
   - 需要删除 `public int HolyWater` 属性
   - 需要删除解析代码中的 HolyWater 行

2. `Assets/AAAGame/Scripts/DataTable/PlayerInitTable.cs`
   - 需要删除 `public int InitHolyWater` 属性
   - 需要删除解析代码中的 InitHolyWater 行

3. `Assets/AAAGame/Scripts/DataTable/PlayerDataTable.cs`
   - 需要删除 `public int MaxHolyWater` 属性
   - 需要删除解析代码中的 MaxHolyWater 行

### 步骤：

1. 打开 Excel 配置表：
   - `AAAGameData/DataTables/PlayerInitTable.xlsx`
   - `AAAGameData/DataTables/PlayerDataTable.xlsx`
   - `AAAGameData/DataTables/PlayerAccountTable.xlsx`

2. 删除这些表中的圣水相关列：
   - PlayerInitTable：删除 `InitHolyWater` 列
   - PlayerDataTable：删除 `MaxHolyWater` 列
   - PlayerAccountTable：删除 `HolyWater` 列

3. 运行 DataTableGenerator 工具重新生成代码

4. 验证编译无错误

---

## 资源管理新设计

### 全局资源（局外）
- ✅ 金币 (Gold)
- ✅ 起源石 (OriginStone)
- ❌ ~~圣水~~ (已移除)

### 战斗资源（局内）
- ✅ 灵力 (Spirit Power)
- ✅ 圣水 (Holy Water) - 战斗时从全局消耗，战斗结束不保留
- ✅ 灵石 (Spirit Stone) - 探索中获得，战斗结束保留

---

## 验证清单

- [x] PlayerAccountData.cs 中删除了 HolyWater 字段
- [x] CurrencyUI.cs 中删除了圣水显示
- [x] PlayerAccountDataManager.cs 中删除了圣水管理方法
- [x] 编辑器面板中删除了圣水编辑
- [ ] 运行 DataTableGenerator 更新自动生成文件
- [ ] 验证编译无错误
- [ ] 测试游戏正常运行

---

## 后续工作

1. **战斗资源管理器** - 需要创建新的 `BattleResourceManager` 来管理战斗中的圣水
2. **圣水消耗逻辑** - 在战斗中使用圣水时，从全局资源消耗
3. **圣水购买** - 在基地商店中用金币购买圣水


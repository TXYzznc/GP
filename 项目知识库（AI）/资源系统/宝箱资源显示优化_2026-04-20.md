# 宝箱资源显示优化

**完成时间**: 2026-04-20  
**状态**: ✅ 优化完成，编译通过

---

## 🎯 优化目标

让金币和灵石也作为虚拟物品显示在宝箱格子里，而不仅仅是直接添加到资源管理器。

---

## ✅ 实现方案

### 物品定义

在 `ItemTable.txt` 中已定义虚拟物品：

| ID | 名称 | 类型 | 图标ID | 说明 |
|-----|------|------|--------|------|
| 999 | 金币 | 0（虚拟） | 1101 | 游戏中的基础货币 |
| 9999 | 起源石 | 0（虚拟） | 1102 | 高级货币 |
| 99999 | 灵石 | 0（虚拟） | 1103 | 战斗资源 |

### 修改逻辑

**文件**: `Assets/AAAGame/Scripts/Game/Interact/Items/TreasureChestInteractable.cs`

#### 金币掉落处理

```csharp
// 处理金币掉落
int totalCoins = 0;
if (Random.value < treasureBoxRow.CoinsProbability)
{
    totalCoins = Random.Range(treasureBoxRow.MinCoins, treasureBoxRow.MaxCoins + 1);
    PlayerAccountDataManager.Instance?.AddGold(totalCoins);

    // 创建金币物品显示在宝箱格子里
    var coinItem = itemManager?.CreateItem(999); // 金币 ID=999
    if (coinItem != null)
    {
        items.Add(new ItemStack(coinItem, totalCoins));
        DebugEx.Log("TreasureChest", $"[GenerateTreasureItems] 生成金币物品: {coinItem.Name} x{totalCoins}");
    }

    // 刷新 CurrencyUI 显示
    string currencyUIAssetName = GF.UI.GetUIFormAssetName(UIViews.CurrencyUI);
    if (!string.IsNullOrEmpty(currencyUIAssetName))
    {
        var currencyUIForm = GF.UI.GetUIForm(currencyUIAssetName);
        var currencyUI = currencyUIForm?.Logic as CurrencyUI;
        if (currencyUI != null)
        {
            currencyUI.RefreshCurrency();
            DebugEx.Log("TreasureChest", "[GenerateTreasureItems] CurrencyUI 已刷新");
        }
    }
}
```

#### 灵石掉落处理

```csharp
// 处理灵石掉落（连续抽奖机制）
int totalMagicaStone = treasureBoxRow.MinMagicaStone;

while (Random.value < treasureBoxRow.MagicaStoneProbability)
{
    totalMagicaStone++;
    if (totalMagicaStone >= treasureBoxRow.MaxMagicaStone)
    {
        totalMagicaStone = treasureBoxRow.MaxMagicaStone;
        break;
    }
}

if (totalMagicaStone > 0)
{
    PlayerRuntimeDataManager.Instance?.AddSpiritStone(totalMagicaStone);

    // 创建灵石物品显示在宝箱格子里
    var spiritStoneItem = itemManager?.CreateItem(99999); // 灵石 ID=99999
    if (spiritStoneItem != null)
    {
        items.Add(new ItemStack(spiritStoneItem, totalMagicaStone));
        DebugEx.Log("TreasureChest", $"[GenerateTreasureItems] 生成灵石物品: {spiritStoneItem.Name} x{totalMagicaStone}");
    }
}
```

---

## 📊 流程图

```
宝箱打开
  ├─ 生成普通物品 → 添加到 items 列表
  ├─ 金币掉落
  │  ├─ 添加到 PlayerAccountDataManager（全局资源）
  │  ├─ 创建金币物品（ID=999）
  │  ├─ 添加到 items 列表（显示在格子里）
  │  └─ 刷新 CurrencyUI
  └─ 灵石掉落
     ├─ 添加到 PlayerRuntimeDataManager（战斗资源）
     ├─ 创建灵石物品（ID=99999）
     └─ 添加到 items 列表（显示在格子里）
```

---

## 🎮 游戏内表现

### 宝箱格子显示

打开宝箱后，格子里会显示：
1. ✅ 普通物品（钱包、药水等）
2. ✅ 金币物品（显示金币图标 + 数量）
3. ✅ 灵石物品（显示灵石图标 + 数量）

### 示例

```
格子0: 钱包 x1
格子1: 生命药水 x1
格子2: 经验宝石 x1
格子3: 金币 x498        ← 新增
格子4: 灵石 x1          ← 新增
```

---

## 📝 日志输出示例

```
[TreasureChest] [GenerateTreasureItems] 生成物品: 钱包 (ID: 1)
[TreasureChest] [GenerateTreasureItems] 生成物品: 生命药水 (ID: 2)
[TreasureChest] [GenerateTreasureItems] 生成物品: 经验宝石 (ID: 4)
[TreasureChest] [GenerateTreasureItems] 生成金币物品: 金币 x498      ← 新增
[TreasureChest] [GenerateTreasureItems] CurrencyUI 已刷新
[TreasureChest] [GenerateTreasureItems] 生成灵石物品: 灵石 x1        ← 新增
[TreasureChest] ═══════════════════════════════════════════════════════
【宝箱掉落统计】可疑的箱子
═══════════════════════════════════════════════════════
📦 物品 (5 个):
   • 钱包 x1
   • 生命药水 x1
   • 经验宝石 x1
   • 金币 x498
   • 灵石 x1
💰 金币: +498
✨ 灵石: +1
═══════════════════════════════════════════════════════
```

---

## 🔑 关键设计

### 虚拟物品的特点

- **类型**: 0（虚拟物品）
- **可堆叠**: 是（最大堆叠数 999999999）
- **可使用**: 否
- **可装备**: 否
- **特殊效果**: 无

### 双重处理

1. **资源管理器**: 金币/灵石同时添加到全局/战斗资源管理器
2. **UI 显示**: 作为物品显示在宝箱格子里

这样既能保证资源的正确管理，又能在 UI 上清晰展示掉落内容。

---

## ✅ 编译状态

- ✅ TreasureChestInteractable.cs - 无错误

---

## 🧪 测试验证

### 预期结果

1. ✅ 宝箱格子显示金币和灵石物品
2. ✅ 金币和灵石图标正确显示
3. ✅ 数量显示正确
4. ✅ CurrencyUI 更新正确
5. ✅ 资源正确添加到管理器

### 测试步骤

1. 打开游戏场景
2. 与宝箱交互
3. 查看宝箱格子中的物品显示
4. 验证金币和灵石是否显示
5. 查看 Console 日志验证流程

---

**优化完成** ✅
